#include <iostream>
using namespace std;

int main() {
    // brute
    temp[]= [1,2,3];
    // shifting
    for (int i = d; i < n; i++)
    {
        a[i-d] = a[i];
    }
    // put back temp
    int j = 0;
    for (int i = n-d; i < n; i++)
    {
        a[i] = temp[j];
        j++
    }
    
    
    cout << "" << endl;
    return 0;
}